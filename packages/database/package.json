{
  "name": "@vision-school/database",
  "version": "1.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx src/migrations/run-migrate.ts",
    "db:seed": "tsx src/seed/run-seed.ts",
    "db:reset": "tsx src/migrations/run-reset.ts",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "@vision-school/shared": "*",
    "@vision-school/config": "*",
    "drizzle-orm": "^0.39.0",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "drizzle-kit": "^0.30.0",
    "tsx": "^4.19.0"
  }
}
